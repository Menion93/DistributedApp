extends layout

block content
    if ingredient._pizzas.length == 0
        The are no pizzas with #{ingredient.name}!
    else
        h1 #{ingredient.name} is present in these pizzas:
        ul
            each pizza in ingredient._pizzas
                li name: #{pizza.name}, description: #{pizza.description}, base: #{pizza.base}
    br
    a(href='/') Torna alla homepage